## TEST CASE

df = session.table("test_table")

df2 = df.with_column("mean", (col("num") + col("num")) / 2)

df3 = df.with_columns(["sum"], [col("num") + col("num")])

df4 = df.with_columns(["sum", "diff"], [col("num") + col("num"), col("num") - col("num")])

df5 = df.rename(col("STR"), "NEW")

df6 = df.rename({col("STR"): "NEW"})

df7 = df.rename({col("STR"): "NEW_STR", "NUM": "NEW_NUM"})

df8 = df.with_column_renamed("STR", "NEW")

df9 = df.with_column_renamed(col("NUM"), "NEW_NUM")

df10 = df.with_column_renamed(col("STR"), "NEW_STR")

## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.with_column("mean", ((col("num") + col("num")) / 2))

res3 = res1.with_columns(["sum"], [(col("num") + col("num"))])

res4 = res1.with_columns(["sum", "diff"], [(col("num") + col("num")), (col("num") - col("num"))])

res5 = res1.rename(col("STR"), "NEW")

res6 = res1.rename({col("STR"): "NEW"})

res7 = res1.rename({col("STR"): "NEW_STR", "NUM": "NEW_NUM"})

res8 = res1.with_column_renamed("STR", "NEW")

res9 = res1.with_column_renamed(col("NUM"), "NEW_NUM")

res10 = res1.with_column_renamed(col("STR"), "NEW_STR")
