## TEST CASE

df = session.sql("select 42")

df2 = session.sql("select * from values (?, ?), (?, ?)", params=[1, "a", 2, "b"])

## EXPECTED OUTPUT

res1 = session.sql("select 42")

res2 = session.sql("select * from values (?, ?), (?, ?)", [1, "a", 2, "b"])
