## TEST CASE

df = session.table("test_table")

df = df.select(-col("A"))

df = df.select(~col("A"))


## EXPECTED OUTPUT

df = session.table("test_table")

df1 = df.select(-col("A"))

df2 = df1.select(~col("A"))
