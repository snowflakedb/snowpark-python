## TEST CASE

df = session.table("test_table")

df = df.select(*).limit(3)

df = df.select(*).limit(n=1, offset=1)

## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.select(res1[*]).limit(3)

res3 = res2.select(res2[*]).limit(1, 1)
