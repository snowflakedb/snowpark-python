## TEST CASE

df = session.table("test_table")

df = df.select(col("*")).limit(3)

df = df.select(col("*")).limit(n=1, offset=1)

## EXPECTED OUTPUT

df = session.table("test_table")

df1 = df.select("*")

df2 = df1.limit(3, 0)

df3 = df2.select("*")

df4 = df3.limit(1, 1)
