## TEST CASE

df = session.create_dataframe([("Paris", "France"), ("Tokyo", "Japan")], ("city", "country"))

df = df.select(create_map("city", "country").alias("map"))

df = df.select(explode(df.map).alias("exploded_map"))

# df = session.table("test_table").select(array_construct(col("num")).alias("cons1"))
#
# df = df.select(explode(df.cons1).alias("exploded1"))

## EXPECTED OUTPUT
