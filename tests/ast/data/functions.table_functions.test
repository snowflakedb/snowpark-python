## TEST CASE

df = session.table("test_table2")

df = df.select(explode(df.maps).alias("emk", "emv"))

## EXPECTED ENCODED AST

## EXPECTED UNPARSER OUTPUT

res1 = session.table("test_table2")

res2 = res1.select(explode(res1["maps"]).alias("emk", "emv"))
