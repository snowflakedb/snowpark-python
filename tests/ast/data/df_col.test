## TEST CASE

df = session.table("test_table")

df = df.select(df.col("STR"), df.STR, df["STR"])


## EXPECTED OUTPUT

df = session.table("test_table")

df1 = df.select(df["STR"], df["STR"], df["STR"])
