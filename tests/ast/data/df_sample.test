## TEST CASE

df = session.table("test_table")

df = df.sample(n=3)

df = df.sample(frac=0.5)

## EXPECTED OUTPUT

df = session.table("test_table")

res1 = df.sample(None, 3)

res2 = res1.sample(0.5, None)
