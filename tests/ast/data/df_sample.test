## TEST CASE

df = session.table("test_table")

df = df.sample(n=3)

df = df.sample(frac=0.5)

## EXPECTED OUTPUT

df = session.table("test_table")

df1 = df.sample(None, 3)

df2 = df1.sample(0.5, None)
