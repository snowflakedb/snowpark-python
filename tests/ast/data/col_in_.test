## TEST CASE

df = session.table("test_table")

df = df.select(col("NUM").in_(1, 2, 3))

df = df.select(col("NUM").in_(df))

df = df.select(col("NUM").in_([1, 2, 3]))

## EXPECTED OUTPUT

df = session.table("test_table")

df1 = df.select(col("NUM").in_(1, 2, 3))

df2 = df1.select(col("NUM").in_(df1))

df3 = df2.select(col("NUM").in_([1, 2, 3]))
