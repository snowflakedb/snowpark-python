## TEST CASE

df = session.table("test_table")

variadic = df.to_df("one", "two")

non_variadic = df.to_df(["one", "two"])


## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.to_df("one", "two")

res3 = res1.to_df(["one", "two"])
