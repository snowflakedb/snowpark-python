## TEST CASE

df = session.table("test_table")

df = df.select(when(col("bool_col"), "true"))

df = df.select(when(col("A") == 1, "one").when(col("A") <= 2, "two").otherwise("other"))

df = df.select(when(col("A").between(37, 42), col("B")).otherwise(col("C")))

df = df.select(when(col("A").equal_nan() , "one").otherwise("other"))

df = df.select(when(col("a").is_null(), lit(1)).when(col("a") == 1, lit(2)).otherwise(lit(3)))

# TODO: SNOW-1546550 fix multiple otherwise calls.
df = df.select(when(col("a").contains("foo"), "foo").otherwise("null").when(col("a").contains("bar"), "bar"))

df = df.select(when(col("a") & col("b"), "both").otherwise("other"))

df = df.select(
    when(col("a") == 1, "one")
    .otherwise("other_one")
    .when(col("a") == 2, "two")
    .otherwise("other_two")
    .when(col("a") == 3, "three")
    .otherwise("other_three")
    .when(col("a") == 4, "four")
    .otherwise("other_four")
    .when(col("a") == 5, "five")
    .otherwise("other_five")
)

## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.select(when(col("bool_col"), "true"))

res3 = res2.select(when((col("A") == 1), "one").when((col("A") <= 2), "two").otherwise("other"))

res4 = res3.select(when(col("A").between(37, 42), col("B")).otherwise(col("C")))

res5 = res4.select(when(col("A").equal_nan(), "one").otherwise("other"))

res6 = res5.select(when(col("a").is_null(), lit(1)).when((col("a") == 1), lit(2)).otherwise(lit(3)))

res7 = res6.select(when(col("a").contains("foo"), "foo").otherwise("null").when(col("a").contains("bar"), "bar"))

res8 = res7.select(when((col("a") & col("b")), "both").otherwise("other"))

res9 = res8.select(when((col("a") == 1), "one").otherwise("other_one").when((col("a") == 2), "two").otherwise("other_two").when((col("a") == 3), "three").otherwise("other_three").when((col("a") == 4), "four").otherwise("other_four").when((col("a") == 5), "five").otherwise("other_five"))
