## TEST CASE

df = session.table(["mock_schema", "\"the#qui.ck#bro.wn#\"\"Fox\"\"won't#jump!\""])
df = df.select("num")

## EXPECTED OUTPUT

df = session.table(["mock_schema", "\"the#qui.ck#bro.wn#\"\"Fox\"\"won't#jump!\""])

df = df.select(col("num"))
