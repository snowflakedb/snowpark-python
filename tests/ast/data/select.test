## TEST CASE

from snowflake.snowpark.functions import col
df = session.table("test_table")
df = df.select("STR", col("A"))

## EXPECTED OUTPUT

res1 = session.table('test_table')

res2 = res1.select(col("STR"), col("A"))
