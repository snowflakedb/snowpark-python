## TEST CASE

df = session.table(["\"the#qui.ck#bro.wn#\"\"Fox\"\"won't#jump!\""])
df = df.select("num")

## EXPECTED OUTPUT

df = session.table(["\"the#qui.ck#bro.wn#\"\"Fox\"\"won't#jump!\""])

df1 = df.select(col("num"))
