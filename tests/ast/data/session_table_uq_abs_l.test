## TEST CASE

df = session.table(["mock_database", "mock_schema", "test_table"])
df = df.select("num")

## EXPECTED OUTPUT

df = session.table(["mock_database", "mock_schema", "test_table"])

df1 = df.select(col("num"))
