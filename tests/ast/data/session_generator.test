## TEST CASE

from snowflake.snowpark.functions import seq1, seq8, uniform

df1 = session.table("test_table")

df2 = session.generator(col("A"), col("B"))

df3 = session.generator(seq1(1), uniform(1, 10, 2), rowcount=3)

df4 = df.generator(seq8(0), uniform(1, 10, 2), timelimit=1)

## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.generator(col("A"), col("B")

res3 = res1.generator(seq1(1), uniform(1, 10, 2), rowcount=3)

res4 = res1.generator(seq8(0), uniform(1, 10, 2), timelimit=1)
