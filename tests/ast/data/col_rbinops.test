## TEST CASE

df = session.table("test_table")

df = df.select(42 + col("B"))

df = df.select(42 - col("B"))

df = df.select(42 * col("B"))

df = df.select(42 / col("B"))

df = df.select(42 % col("B"))

df = df.select(42 ** col("B"))

df = df.select(42 & col("B"))

df = df.select(42 | col("B"))


## EXPECTED OUTPUT

df = session.table("test_table")

df = df.select((42 + col("B")))

df = df.select((42 - col("B")))

df = df.select((42 * col("B")))

df = df.select((42 / col("B")))

df = df.select((42 % col("B")))

df = df.select((42 ** col("B")))

df = df.select((42 & col("B")))

df = df.select((42 | col("B")))
