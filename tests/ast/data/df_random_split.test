## TEST CASE

df = session.table("test_table")

weights = [0.1, 0.2, 0.3]

df2 = df.random_split(weights)

df3 = df.random_split(weights, seed=24)

## EXPECTED OUTPUT

df = session.table("test_table")

df2 = df.random_split([0.1, 0.2, 0.3], None)

df3 = df.random_split([0.1, 0.2, 0.3], 24)
