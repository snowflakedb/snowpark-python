## TEST CASE

df = session.table("test_table")

getitem = df.select(df["STR"])

getattr = df.select(df.STR)

col = df.select(df.col("STR"))

## EXPECTED OUTPUT

res1 = session.table("test_table")

res2 = res1.select(res1["STR"])

res3 = res1.select(res1["STR"])

res4 = res1.select(res1["STR"])
